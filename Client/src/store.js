import { createStore } from "vuex";

const store = createStore({
  state: {
    
  },
  mutations: {


    // checkUser({ commit }, { userEmail, token }) {
    //     fetch('/api/check-user', {
    //       method: 'POST',
    //       headers: {
    //         'Content-Type': 'application/json',
    //       },
    //       body: JSON.stringify({ email: userEmail, token })
    //     })
        
    //  }
     
  },
  getters: {
   
  },
});

export default store;
